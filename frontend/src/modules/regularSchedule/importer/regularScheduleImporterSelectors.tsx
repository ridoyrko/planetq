import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const regularScheduleImporterSelectors = importerSelectors(
  'regularSchedule.importer',
);

export default regularScheduleImporterSelectors;
